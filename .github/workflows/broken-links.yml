name: 🔗 Kontrola interních odkazů

on:
  # Umožní manuální spuštění z karty "Actions"
  workflow_dispatch:
    inputs:
      sitemap_url:
        description: 'URL sitemapy ke kontrole (např. https://vas-web.cz/sitemap.xml)'
        required: true

  # Spustí skript automaticky každé pondělí v 6:00 UTC (8:00 našeho letního času)
  schedule:
    - cron: '0 6 * * 1'

jobs:
  check-links:
    runs-on: ubuntu-latest

    steps:
      - name: 1. Stáhnout kód repozitáře
        uses: actions/checkout@v4

      - name: 2. Nastavit Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 3. Instalovat potřebné knihovny
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 lxml

      - name: 4. Spustit kontrolu (při manuálním spuštění)
        # Tento krok se spustí, jen pokud byla akce vyvolána ručně
        if: github.event_name == 'workflow_dispatch'
        run: |
          python check_links.py ${{ inputs.sitemap_url }}

      - name: 4. Spustit kontrolu (při plánovaném spuštění)
        # Tento krok se spustí, jen pokud byla akce vyvolána plánovačem (schedule)
        if: github.event_name == 'schedule'
        run: |
          # Pro plánované spuštění použijeme URL z "Repository Variables"
          # Musíte nastavit proměnnou SITEMAP_URL_PROD ve vašem repozitáři
          python check_links.py ${{ vars.SITEMAP_URL_PROD }}
