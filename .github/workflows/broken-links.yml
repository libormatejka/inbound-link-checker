name: 🔗 Kontrola interních odkazů

on:
  # Umožní manuální spuštění z karty "Actions"
  workflow_dispatch:

  # Spustí skript automaticky každé pondělí v 6:00 UTC
  schedule:
    - cron: '0 6 * * 1'

jobs:
  check-links:
    runs-on: ubuntu-latest

    steps:
      - name: 1. Stáhnout kód repozitáře
        uses: actions/checkout@v4

      - name: 2. Nastavit Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 3. Instalovat potřebné knihovny
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 lxml

      - name: 4. Spustit kontrolu odkazů
        # Tento krok se spustí vždy, bez ohledu na trigger
        # a vždy použije proměnnou z nastavení repozitáře
        run: |
          python check_links.py ${{ vars.SITEMAP_URL_PROD }}
